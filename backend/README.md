# Backend

Not implemented yet. Planned: Node.js + Express API over the MySQL `lms`
database, mirroring the reducer actions in `frontend/src/store/AppContext.jsx`.

Tables to create: `subjects`, `modules`, `instructors`, `students`, `reviews`,
`slots`, `slot_requests`, `group_classes`, `enrollments`, `payments`.

Field shapes are documented by the demo dataset in
`frontend/src/data/seed.js`.
